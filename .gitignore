# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Environment variables (contains secrets)
.env
.env.local
.env.*.local

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Database backups (contain sensitive auth data)
database_backups/

# Claude Code per-developer settings (permissions, approvals)
# Shared .claude/settings.json stays tracked; this local override does not.
.claude/settings.local.json

# Playwright E2E test outputs and auth state
/test-results/
/playwright-report/
/playwright/.cache/
/tests/e2e/.auth/

# Compound-engineering pipeline artifacts (ce-review JSON, run scratch)
.context/

# Git worktrees (per-developer)
.worktrees
