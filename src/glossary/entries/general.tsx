/**
 * @fileoverview Cross-cutting glossary entries that don't fit a single
 * Module bucket (e.g., Division, Module, Spot, Win).
 *
 * Empty placeholder seeded in Unit 1. Filled in Units 4 + 5 as the league-v2
 * wizard and operator screens are walked for coverage.
 */

import type { GlossaryEntry } from '../types';

export const entries = {} as const satisfies Record<string, GlossaryEntry>;
