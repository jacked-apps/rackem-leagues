/**
 * @fileoverview DummyFormData — shared type for the dummy test wizard
 *
 * Temporary. Deleted in Phase 5 when real league wizard steps replace
 * the dummy steps.
 */

export interface DummyFormData {
  'dummy-text-step'?: string;
  'dummy-choice-step'?: string;
}
